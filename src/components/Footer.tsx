import React from "react";
import { AppBar, Toolbar, Typography } from "@mui/material";

const Footer: React.FC = () => (
  <AppBar position="static">
    <Toolbar>
      <Typography variant="h6">Meduzzen, 2024</Typography>
    </Toolbar>
  </AppBar>
);

export default Footer;
