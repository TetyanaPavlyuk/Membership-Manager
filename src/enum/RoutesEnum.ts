export enum RoutesEnum {
  HOME = "/",
  ABOUT = "/about",
  USERS = "/users",
  COMPANIES = "/companies",
  REGISTRATION = "/registration",
  LOGIN = "/login",
  LOGIN_SOCIAL = "/login-social",
  DASHBOARD = "/dashboard",
}
